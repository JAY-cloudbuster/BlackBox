const { processDocumentText } = require('./server.js'); // Assuming processDocumentText is exported or we can just copy it
// Oh wait, processDocumentText is not exported. Let me just run server.js in another process and hit the upload endpoint via curl or let Node crash it.
